<Window x:Class="TickyNote.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:TickyNote.Controls"
        mc:Ignorable="d"
        Title="TickyNote"
        Height="330" Width="460"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        Topmost="True"
        Icon="/Assets/app-icon.ico">

    <Border CornerRadius="14"
            Background="{Binding ThemeColor}"
            Padding="12"
            BorderThickness="1"
            BorderBrush="{StaticResource BorderSubtleBrush}"
            MouseLeftButtonDown="DragWindow">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <controls:TitleBarControl Grid.Row="0"/>
            <controls:TimerListControl Grid.Row="1"/>
            <controls:TimerInputControl Grid.Row="2"/>
        </Grid>
    </Border>

</Window>
